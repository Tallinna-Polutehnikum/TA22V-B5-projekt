import {describe, test, jest, expect } from "@jest/globals";
import LanguageRepository from "../repositories/languageRepository";

describe('LanguageRepository testing', () => {
    test('test desc', async () => {
        //DB mock creation
        const dbMock = jest.mocked(db);
    })
})